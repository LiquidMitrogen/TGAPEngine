#include "FftTimerEvent.h"


void FftTimerEvent::callMe(){/*
    if(running == false){
        if (mic->maxs > 0.3) running = true;
    }else{
        //std::cout<<std::endl<<"derp "<<floor(engine::interpf(1.0,60.0,(this->time - this->tLeft) / this->time))<<std::endl;
        entity->setAmimationFrame(floor(engine::interpf(1.0,60.0,(this->time - this->tLeft) / this->time)));
        entity->calcBoneUniforms();
        if(this->tLeft < 1) running = false;
//mat->setUniformColor(glm::vec4(engine::interpf(1.0,0.0,(this->time - this->tLeft) / this->time),0.0f,0.0f,1.0f));
}*/
}

